import 'package:flutter/material.dart';

class PalletAssignmentScreen extends StatelessWidget {
  const PalletAssignmentScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Place Pallet on Rack')),
      body: const Center(
        child: Text('Pallet Assignment UI goes here.'),
      ),
    );
  }
}
